import NotFoundBody from "@/components/NotFoundBody";

/** A 404 raised by a page in this group, so it keeps this group's header and footer. */
export default function NotFound() {
  return <NotFoundBody />;
}
